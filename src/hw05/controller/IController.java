package hw05.controller;

/**
 * Represents a way for the user to interact with the picture editor.
 */
public interface IController {

  /**
   * Sets up and starts the editor, allowing the user to input commands to manipulate it, and
   * displaying feedback in the console.
   */
  void useEditor();
}
